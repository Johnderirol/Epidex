{% extends 'base.html.twig' %}

{% block title %}CompEtoile{% endblock %}

{% block body %}
<div class="container">
    <h1>CompEtoile</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ comp_etoile.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ comp_etoile.title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ comp_etoile.description }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ comp_etoile.slug }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('compEtoile_index') }}">back to list</a>

    <a href="{{ path('compEtoile_edit', {'id': comp_etoile.id}) }}">Modifier</a>
</div>

{% endblock %}
