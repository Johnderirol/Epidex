{% extends 'base.html.twig' %}

{% block title %}MissionCible index{% endblock %}

{% block body %}
<div class="container">
    <h1>MissionCible index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Title</th>
                <th>Compétences</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for mission_cible in mission_cibles %}
            <tr>
                <td>{{ mission_cible.title }}</td>
                <td>
                {% for competences in mission_cible.competences %}
                    {{competences.title}}
                {% endfor %}
                <td>
                    <a href="{{ path('mission_cible_show', {'id': mission_cible.id}) }}" class="btn btn-success btn-xs" title="Voir"><i class="fas fa-eye"></i></a>
                    <a href="{{ path('mission_cible_edit', {'id': mission_cible.id}) }}" class="btn btn-primary btn-xs" title="Modifier"><i class="fas fa-edit"></i></a>
                    <a href="{{ path('mission_cible_delete', {'id': mission_cible.id}) }}" class="btn btn-danger btn-xs" title="Supprimer"><i class="fas fa-trash"></i></a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    <a href="{{ path('mission_cible_new') }}">Create new</a>
</div>
{% endblock %}
