{% extends 'base.html.twig' %}

{% block title %}CompLeader{% endblock %}

{% block body %}
    <h1>CompLeader</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ comp_leader.id }}</td>
            </tr>
            <tr>
                <th>Pole</th>
                <td>{{ comp_leader.pole }}</td>
            </tr>
            <tr>
                <th>Definition</th>
                <td>{{ comp_leader.definition }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('comp_leader_index') }}">back to list</a>

    <a href="{{ path('comp_leader_edit', {'id': comp_leader.id}) }}">edit</a>

    {{ include('comp_leader/_delete_form.html.twig') }}
{% endblock %}
